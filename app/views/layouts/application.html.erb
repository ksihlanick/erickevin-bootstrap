<!DOCTYPE html>
<html lang="en">
<head>
  <title>The Ice Cream Truck | Ice Cream Enthusiants Ice Cream Club</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>
  <meta name="description" content="Your Ice cream club of the month delivering the best ice cream, frozen yogurt, Sundaes, Ice Cream sandwiches, and frozen treats straight to your door.">
</head>
<body>
  <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
</body>
</html>
